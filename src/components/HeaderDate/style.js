import styled from 'styled-components';

export const DateContainer = styled.div`
	span {
		color: ${({ theme }) => theme.palette.darkblue};
		font-size: ${({ theme }) => theme.fontSizes.medium};
		/* font-family: 'Kyobohandwriting2021syj'; */
	}
`;
